# Internal API Key for scheduler and other internal services
X_INTERNAL_API_KEY=

# Supabase credentials (only required for production)
SUPABASE_URL=
SUPABASE_KEY=

# Redis connection (optional, defaults to localhost:6379)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Server port (optional, defaults to 3000)
PORT=3000
